{
    "ownUrl": "${OWN_URL}",
    "apiUrl": "${API_SERVICE_URL}",
    "authUrl": "${AUTH_SERVICE_URL}",
    "usersManagementUrl": "${USERS_MANAGEMENT_SERVICE_URL}",
    "signOn": {
      "authority": "${SIGNON_AUTHORITY}",
      "clientId": "${SIGNON_CLIENT_ID}"
    },
    "keycloakConfig": {
      "issuer": "${KEYCLOAK_ISSUER}",
      "clientId": "${KEYCLOAK_CLIENT_ID}",
      "silentRefreshRedirectUri": "${KEYCLOAK_REFRESH_REDIRECT_URI}",
      "dummyClientSecret": "${KEYCLOAK_DUMMY_CLIENT_SECRET}"
    }
}
